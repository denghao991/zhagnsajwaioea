# 面试追问清单
❯ 再开辩论agent,我现在是后端开发,准备转agent开发  注意岗位和岗位的要求吧                                                                            
## 项目1：分布式可用性检查任务调度引擎 — 技术深度

| # | 问题 | 考察点 |
|---|------|--------|
| 1 | ZK Watcher 是一次性触发的，触发后必须重新注册。你的代码里是怎么处理的？用的 Curator PathChildrenCache 还是自己每次重新注册？ | 是否真的写过 ZK 代码 |
| 2 | 如果一个 Client 正在执行5个任务时 FGC 导致 ZK 会话超时，临时节点被删，Leader 判定它宕机把任务分给了别的节点。然后原 Client GC 结束、会话恢复，它还在执行那5个任务——同一个任务被两个节点同时执行了。你怎么防止这种脑裂？ | 分布式并发控制 |
| 3 | Leader 自己宕机了怎么办？新 Leader 被选出来以后，怎么处理 `/failover` 目录里的半成品数据？三阶段之间 Leader 挂了，怎么保证不出现双写或漏写？ | 故障转移的一致性 |
| 4 | 你说用了 LeaderLatch，如果 Leader 长时间 GC 没宕机，hasLeadership() 在 GC 恢复后返回什么？你怎么检测自己已经不是 Leader 了？ | Curator 源码理解 |
| 5 | 分配时是"低于阈值随机选"，这个阈值怎么定的？如果所有节点都低于阈值，随机分布本身就不均衡了怎么办？如果所有节点都超阈值，是拒绝分配还是硬着头皮分？ | 算法边界条件 |
| 6 | 所有 Job 都是持久节点吗？如果是持久节点，谁清理已完成的 Job？你的清理逻辑里，如果清理线程和分配线程并发操作同一个 Job 节点，会有什么问题？ | ZK 节点生命周期 |
| 7 | ZK 集群几个节点？session timeout 设的多少？有没有遇到过网络分区导致的 false positive（ZK 误判 Client 宕机）？ | 运维经验 |
| 8 | 为什么选"每5分钟拉取 Redis"而不是"Client 直接从 Redis 竞争消费"？中间多了一层 Leader 分发，优势在哪？ | 架构决策 |

## 项目1：分布式可用性检查任务调度引擎 — 方案选型

| # | 问题 | 考察点 |
|---|------|--------|
| 9 | 你说 Redis Stream 依赖 Redis 6.0，但其实 Stream 从 5.0 就有了，XAUTOCLAIM 是 6.2 引入的。你当时调研的是哪个特性不够用？ | 方案对比是否真实 |
| 10 | Redis List 的 RPOPLPUSH 做可靠队列根本不需要 Leader 选举——每个 consumer 自己 RPOPLPUSH，独立的 monitor 线程回收超时任务就行。你为什么认为它需要 Leader？ | 是否过度设计了方案选型 |
| 11 | 每日 5000 任务，每分钟 3.5 个，这种量级一个 PostgreSQL 的 SELECT FOR UPDATE SKIP LOCKED 就能做任务队列。为什么不上更简单的方案？ | 是否"杀鸡用牛刀" |
| 12 | Kafka 方案里你说消费者和分区静态绑定无法动态分配——但如果把分区数设大、通过暂停/恢复特定消费者来模拟动态负载均衡呢？你们讨论过这个方案吗？ | 方案对比的深度 |

## 项目2：检查项平台化对接

| # | 问题 | 考察点 |
|---|------|--------|
| 13 | 算子和策略之间的接口契约是什么？一个策略怎么声明它需要哪些算子？算子的输入输出格式是什么？ | 接口设计 |
| 14 | SRE 写完策略怎么测试？有沙箱环境吗？策略上线后出错了怎么排查——日志在哪、谁收到告警？ | 可观测性 |
| 15 | 你说回调幂等是"先查库判断是否已处理，未处理则从先知拉结果"，但如果两个并发回调同时到达，两个请求都查到"未处理"，你怎么防？数据库唯一索引？分布式锁？ | 并发安全 |
| 16 | 先知平台队列满或拒绝任务时，你们怎么感知？感知到之后做什么？重试？降级？告警？ | 异常处理 |
| 17 | 先知和 TOS 两个平台，一个用回调、一个用轮询。如果让你重新设计，你会把两者统一成什么模式？ | 抽象能力 |
| 18 | 检查项执行结果的数据流：ZK 里存 Accept 标记，数据库里存结果，前端怎么展示？三者之间的关联字段是什么？ | 数据一致性 |

## 项目3：企业级 RAG 知识库系统（tiny-rag）

| # | 问题 | 考察点 |
|---|------|--------|
| 19 | 你做的是中文文档场景，Embedding 选型时你怎么评估不同模型？用了什么评估指标？最终为什么选 text-embedding-v3-512d 而不是 v2 或者更大的维度？ | Embedding 选型方法论 |
| 20 | RRF 融合里 α 和 β 权重你是怎么确定的？如果 α=6/β=4 的 MRR 是 0.772，改成 α=5/β=5 是多少？有没有做过网格搜索？ | 检索排序调优 |
| 21 | 语义缓存的阈值为什么设 0.03？设太低了命中率低、设太高了容易返回不相关的结果——你是怎么在召回率和准确率之间平衡的？ | 缓存设计 |
| 22 | BM25 的索引在每次 add_document 时全量重建——如果你有 10 万个文档，这个方案还 work 吗？替代方案是什么？ | 扩展性思考 |
| 23 | 你的 Chunk 大小是怎么定的？为什么是 512 而不是 384 或 1024？你做了什么实验来验证？ | Chunk 策略 |
| 24 | RAG 系统里最头疼的是 retrieved context 不相关导致 LLM 答非所问。你的系统里怎么应对这个问题？做了什么来保障检索质量？ | RAG 核心难点 |

## 综合 / 转行

| # | 问题                                                                       | 考察点 |
|---|--------------------------------------------------------------------------|--------|
| 25 | 你 3 年 Java 后端，现在投 AI 应用开发——为什么转？你为这个方向做了哪些准备？                            | 动机与准备 |
| 26 | 你觉得分布式调度框架里的事件驱动+Leader 协调模式，跟 AI Agent 的任务编排有什么相通之处？                    | 知识迁移能力 |
| 27 | tiny-rag 是你用 AI 辅助编程做的，你在这个过程中怎么保证代码质量？AI 生成的代码你改了多少？                    | 使用 AI 工具的成熟度 |
| 28 | 如果一个 Agent 需要调用10个外部 API、每个都可能超时或返回异常，你会怎么设计它的容错机制？从你在调度框架里的经验出发，你会借鉴什么？ | 跨领域架构能力 |