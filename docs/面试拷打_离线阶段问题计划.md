# 面试拷打 —— 离线阶段问题计划

> 面试官视角：基于你提交的三份面试准备文档，对离线工程阶段进行严格追问。
> 每个批次包含3-5个主问题 + 预设追问角度。你回答完一批后我会逐条追问。

---

## 第一批：分块策略与评估方法论

### Q1.1 — chunk_size 你到底定的是多少？证据链完整吗？

你在三份文档里对 chunk_size 的描述存在多处不一致：

- **系统架构**说 chunk_size=512，理由是"碎片率0%，填充率87.9%"
- **系统架构**的评估表里 512 的 fill_rate_p50=48.5%，标注为 ❌ 不合格；但结论却说"512 最优"
- **深度解析**说 overlap=64（默认值），**全景描述**却说 overlap=0 并长篇论证了"为什么要设0"
- 评估表的128/256/384/512四组数据，与全景描述里声称的"对比了256/512/768/1024/1536五种配置"对不上

**请你说清楚：**
1. 你最终上线的 chunk_size 和 overlap 到底是多少？
2. 评估表里 512 的 fill_rate_p50=48.5% 被标记为 ❌，为什么结论却是 512 最优？这个推理链路是什么？
3. overlap 从 64 改到 0 是你真的测过，还是只是文档里写的"论证"？

---

### Q1.2 — 你的 chunk_size 评估到底覆盖了多少文档？

- 系统架构说"32 chunks 评估集"
- 全景描述说"50 条真实 QA 对"
- 深度解析说 chunk_size 评估用了"碎片率+P75延迟+人工抽检"

**请你说清楚：**
1. 这个评估集到底有几个文档、几个 chunk？
2. 32 个 chunk 的规模，你算出来的 fill_rate 和 fragmentation rate 有统计意义吗？如果我给你 100 倍的文档量，结论会不会变？
3. 你说"人工抽检语义完整性"，抽检了多少条？谁抽检的？判定标准是什么？

---

### Q1.3 — 标题路径拼接在分块前面还是后面？

你的文档说：分块完成后"在块前拼接标题路径（如 `产品介绍 > 核心功能 > ……`）"。

**请你说清楚：**
1. 标题路径拼接后，chunk 的 token 数是不是就变了？你是在拼接前按 chunk_size 控制，还是拼接后？
2. 如果拼接后超出了 chunk_size，你怎么处理？有没有可能出现"标题路径占了 200 token，正文只剩 312 token"的情况？
3. 这对 BM25 检索有什么影响——BM25 看到的前几个词永远是标题路径，这不是在浪费关键词权重吗？

---

## 第二批：RRF 权重 —— 你的核心叙事经得起推敲吗？

### Q2.1 — alpha/beta 到底是多少？你改过吗？

这是你三份文档里最大的矛盾点：

- **系统架构**：alpha=7, beta=3
- **深度解析**：alpha=3, beta=10（并详述了"从7/3改为3/10"的演化历史）
- **全景描述**：alpha=7, beta=3

深度解析专门画了表：

| 版本 | alpha | beta | 问题 |
|------|-------|------|------|
| 初始 | 7.0 | 3.0 | BM25 永远进不了 top 10 |
| 调整后 | 3.0 | 10.0 | BM25 精确匹配有竞争力 |

**请你说清楚：**
1. 你当前系统实际运行的是哪个权重？
2. 如果你真的从 7/3 改到了 3/10，改动前后的评估数据是什么？Recall@K 和 MRR 变了多少？
3. 如果你没改过，为什么要在文档里写这个"演化历史"？

---

### Q2.2 — 你的 RRF 权重评估能区分不同配置吗？

你自己在系统架构里写了这段话：

> "当前 VECTOR_N=12, BM25_N=4 下，BM25 rank 1 得分 = 3/61 ≈ 0.049，向量 rank 12 得分 = 7/72 ≈ 0.097，意味着 BM25 独立命中的结果排不进前 10"

深度解析里也说：

> "当前测试集太小（13 chunks, 4 QA pairs），BM25 无独家命中，导致所有权重组合的 Recall/MRR 完全相同"

**请你说清楚：**
1. 既然评估集上所有权重组合的 Recall/MRR 完全相同，你是凭什么选的 7/3（或 3/10）？
2. 你文档里写的"alpha=7, beta=3 是最优配置"，这个"最优"的证据是什么？
3. 你说"BM25 独立命中排不进 top 10"——那你加 BM25 这条路的意义是什么？如果它只能当辅助叠加而不能独立贡献，为什么要加？

---

### Q2.3 — RRF 的 K=60 你验证过吗？

你说 K=60 是"RRF 论文建议的经验最优值"。

**请你说清楚：**
1. 你试过 K=30 或 K=90 吗？在你自己的数据上 K 值敏感吗？
2. RRF 论文的结论是在英文 TREC 数据集上的，你确定它迁移到中文技术文档 + 双路检索场景下还有效？
3. 如果 K=10（小值），top 结果权重极高，BM25 top-1 的贡献会大幅提升——这不正是你想要的效果吗？为什么不试试？

---

## 第三批：Embedding 选型 —— 你的结论和行动对不上

### Q3.1 — 你到底选了哪个 Embedding 模型？

- **全景描述**用大量篇幅描述了 text-embedding-v2 的选型理由：v3 降维版 Recall 不如 v2、bge 太重要 GPU、v2 的 1536 维"维度适中"
- **系统架构**却说 embedding 状态是"🔄 待切 v3(dim=512)"
- **深度解析**说评估结论是"v3(dim=512) 最优，R@1=0.786"

三个说法互斥。

**请你说清楚：**
1. 评估脚本跑出来的最优模型，和实际部署的模型，是不是同一个？
2. 如果最优是 v3，为什么线上还跑着 v2？
3. "待切换"是什么在阻塞你？API 申请？兼容性问题？还是评估结论自己也不确定？

---

### Q3.2 — 你的 Embedding 评估能支撑选型吗？

你说评估了 v1/v2/v3 三个版本，评估集是"14 条 QA pairs"（系统架构）或"50 条 QA 对"（全景描述）。

**请你说清楚：**
1. 评估集到底多少条？14 还是 50？
2. 如果只有 14 条，你在 14 条上看到 v3 的 R@1 比 v2 好 1 个百分点，这个差异有统计显著性吗？
3. 14 条样本上跑 4 个候选模型，多重比较问题你考虑过吗？

---

### Q3.3 — 失败 case 分析你说得头头是道，但驱动了什么？

全景描述里有一段精彩的失败 case 分析：
> "数字/代码片段：向量无法区分 max_connections=100 和 500"
> "否定语义：'不支持 XX'和'支持 XX'的余弦距离很近"
> "缩写歧义：OA 在不同上下文可能不同"

然后你说"这些发现驱动了多路召回设计"。

**请你说清楚：**
1. 这个失败分析是在 embedding 选型阶段做的，还是后来补写的？
2. 如果是选型阶段做的——既然所有 embedding 模型在这些 case 上都有系统性缺陷，你选哪个模型有区别吗？
3. 你具体有没有针对这三个缺陷做过任何定量测量？比如"否定语义的误召回率是 X%"？

---

## 第四批：BM25 与双路检索的工程细节

### Q4.1 — jieba 分词不做领域定制，你能接受吗？

你的文档里完全没有提到 jieba 自定义词典。你的业务文档包含大量内部术语：`多AZ`、`RDS推荐器`、`扩容逻辑`、`etcd集群`。

**请你说清楚：**
1. jieba 默认分词下，"CSS可用区未多AZ"会被切成什么？你验证过吗？
2. `多AZ` 如果被切成 `["多", "AZ"]`，BM25 还能召回到相关内容吗？
3. 你为什么没做领域词典？是没意识到、觉得不重要、还是做了但文档没写？

---

### Q4.2 — BM25 每次 add 全量重建，你到底测过性能吗？

你文档里反复说"数据量小（<1000 chunks），重建耗时 <50ms"，但在另一处又说"文档量大时效率低（每次 add_document 重建全量索引）"列为已知局限。

**请你说清楚：**
1. <50ms 是你实测的数据还是估算？
2. 你实际测试环境里有多少个 chunk？重建确实只要 50ms？
3. 如果用户一次上传了一个被切成 500 个 chunk 的大文档，这 500 个 chunk 会触发 500 次全量重建吗？还是批量 add？如果不是 500 次，那总重建开销是多少？

---

### Q4.3 — 向量路用改写后的问题，BM25 路用的又是什么？

- 系统架构的流程图里：向量检索用的是 `question_vec`（改写后的 embedding），BM25 检索用的是 `question`（原始）
- 深度解析也说：BM25 路用原始问题

**请你说清楚：**
1. 为什么向量用改写版、BM25 用原始版？这个不对称设计是你刻意设计的还是无意的？
2. 如果查询改写把"OA"展开成了"优化顾问(OA)"，BM25 用原始版"OA有哪些功能"去搜——OA 不就只能命中缩写原文了吗？这是好是坏？
3. 你有没有做过对比：改写版 vs 原始版分别给 BM25 用，哪个召回更好？

---

## 第五批：缓存设计 —— 看似完善实则有很多没说的

### Q5.1 — 上传一个新文档就清空全部缓存，你确定这是最优策略？

你的缓存策略是：上传任意一个文档 → `cache.clear()` 清空所有缓存。你自己也说这是"改进方向"。

**请你说清楚：**
1. 假设知识库有 100 篇文档，缓存里积累了 500 条经过真实用户验证的有效回答。用户上传了一篇与这些回答完全无关的新文档——500 条缓存全部清空。这个代价你算过吗？
2. "按文档 ID 选择性淘汰"在技术上的难点是什么？为什么不实现？
3. 你说缓存清空是因为"全量清空更安全"——你有没有量化过误命中的风险和全量清空的代价？

---

### Q5.2 — threshold=0.03 是"在 100 对问题上做 F1 评估"得出的，但你写的评估方法在哪儿？

全景描述说：
> "通过人工抽检 100 对问题，标注'是否语义相同'，在 0.01/0.03/0.05/0.07 四个阈值下计算 F1。0.03 取得了最高的 F1（~0.91）"

**请你说清楚：**
1. 这 100 对问题是从哪儿来的？是真实用户问题还是你自己构造的？
2. 你怎么定义"语义相同"？"OA有什么功能"和"优化顾问能做什么"算相同还是不同？
3. F1=0.91 的精确率和召回率分别多少？在 RAG 缓存场景下，你更关心精确率（防止误命中）还是召回率（提高命中率）？
4. 你把这个评估结果放在缓存设计文档里——但 100 对问题、4 个阈值的人工评估，这个结论跟着上线后的真实流量走，你敢赌它不变吗？

---

### Q5.3 — force_refresh 重新生成后不覆盖旧缓存，为什么？

文档说：
> "如果点击重新生成，说明语义匹配了缓存但是实际语义没有解决用户需求，直接走正常流程，新建一条缓存"

**请你说清楚：**
1. 旧缓存还在，下次有人问类似的问题还是会命中那个"已被用户判定为无用"的旧缓存，这不是让 force_refresh 变成一个无限循环吗？
2. 你为什么不直接覆盖或删除那条旧缓存？
3. cache entry 的 `entry_id` 是随机生成的，你怎么知道 force_refresh 触发时该删除哪个旧缓存？

---

## 第六批：配置体系与查询改写

### Q6.1 — 双层配置系统：.env + config.yaml，为什么？

- `.env` 用 pydantic-settings 读 API key 和基础设施参数
- `config.yaml` 用 `_reload_config()` 读检索参数和术语映射
- 但 chunk_size 和 chunk_overlap 在 `.env` 里，不在 config.yaml 里

**请你说清楚：**
1. 哪些参数放 .env、哪些放 YAML 的分类标准是什么？
2. chunk_size 在 .env 里但 VECTOR_N 在 YAML 里——它俩都是检索参数，为什么分开放？
3. YAML 是"热加载"（下次请求生效）、.env 是"冷加载"（需重启）——如果有人改了 YAML 里的 alpha 值但没重启，系统行为会不一致吗？

---

### Q6.2 — 查询改写的鲁棒性

你的 TERM_MAP 只有 3 条映射（OA、CSS、CCE），REWRITE_EXAMPLES 只有 2 条示例。

**请你说清楚：**
1. 只有 3 条映射的 TERM_MAP，实际能起到多大作用？
2. 如果用户问了一个包含不在 TERM_MAP 中的缩写的问题（比如 "EVS"），LLM 会怎么改写？你能保证改写正确吗？
3. REWRITE_PATTERN 里写的是"云服务名+检查项描述"，如果用户问的不是检查项（比如"OA的监控告警怎么配"），这个 pattern 还适用吗？
4. 改写失败（API 超时/报错）时你 return 原始问题——如果这发生在高负载下，大量问题未被改写就直接检索，召回质量会下降多少？

---

## 第七批：可观测性与评估的"水分"

### Q7.1 — QueryLog 只写不查，那你日志里到底发现了什么？

你的 QueryLog 设计原则是"只写入，不查询"，说这样"模块职责极简"。但你在文档里又反复提到日志用于"离线分析检索质量"。

**请你说清楚：**
1. 你实际做过离线分析吗？从日志里发现了什么问题？
2. 如果一次都没分析过，你怎么知道日志字段设计得合理？
3. `user_click` 列预留了但从未使用——你为什么确定这个字段的设计是对的？前端如果真的接入点击反馈，`user_click` 存什么格式的数据？
4. 日志里记录了 `vector_hits` 和 `bm25_hits` 的 chunk_id 数组，要还原具体内容需要 JOIN chunk_registry.json——这个关联查询你实际跑过吗？

---

### Q7.2 — 评估集的规模与泛化

你的评估数据存在多处不一致：
- RRF 评估：14 条 QA 或 4 条 QA
- Embedding 评估：14 条或 50 条
- chunk_size 评估：32 chunks

**请你说清楚：**
1. 你的三份文档里没有一个统一的评估集规模数字，你自己能准确说出每个评估环节的真实数据量吗？
2. 如果我问你：在这个评估集上调出来的 alpha=7, beta=3，放到真实场景里会有什么问题——你怎么回答？
3. 你说 next step 包括"基于完整库复验 RRF 权重"，如果完整库上线后评估结果和现在完全不同，你之前写在文档里的那些"选型结论"算什么？

---

### Q7.3 — 测试覆盖的真实情况

深度解析 Q13 说"有 15 个测试文件"，但你自己也承认：
- 核心 `/ask` SSE 路由全部 mock 了外部 API
- 需要 API key 的测试在 CI 中被跳过
- 无 SSE 断连/超时等边界测试
- 无压力测试

**请你说清楚：**
1. 如果 API mock 了，你怎么保证 mock 的行为和真实 API 一致？
2. `cache.search()` 返回 `None` 时，`/ask` 的逻辑正确吗？你测过吗？
3. 当 Rerank API 返回格式与预期不符时，你的 fallback 逻辑有没有被测试覆盖？
4. 你文档里写了"优雅降级是设计原则"，但优雅降级路径有测试吗？

---

## 拷打计划执行方式

每个批次我会：
1. 先抛出该批次全部主问题
2. 等你逐个回答
3. 针对你的回答进行逐条追问（追问角度已在每个问题下列出）
4. 你的回答不充分时我会反复深挖，不让你糊弄过去

准备好了告诉我，我们从第一批开始。